{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "claude-coord",
  "description": "Coordenação multi-agente entre Claudes — salas separadas + handoff conflict-free + recebimento via watcher (Monitor) + aviso passivo",
  "owner": {
    "name": "wansolanso"
  },
  "plugins": [
    {
      "name": "coord",
      "source": "./",
      "description": "Coordenação multi-agente entre Claudes: salas separadas + mensagens conflict-free + recebimento via watcher (Monitor) + aviso passivo"
    }
  ]
}
